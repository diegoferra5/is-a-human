# Listen: `call_6971b2685c1d`

Val human. Both heads wrong (`miss_agree`). fused 0.834 · acoustic 0.860 · behavioural 0.575.

97 s stereo. Caller speaks **19 s** across 10 VAD turns; agent speaks **51 s**. Quiet caller (speech RMS 0.03 vs a typical synth 0.21).

## What the recording does

Four real waits after the agent stops: **1.50, 1.70, 1.79, 2.58 s**. Humans on val wait 1.22 s median and often snap back in ~0.7 s. This caller never does. One tiny start-of-call overlap; essentially no barge-ins or backchannels (1 vs human means ~8–9).

The close spectrogram of the 9.4–14.7 s utterance has real harmonic stacks and internal pauses — it sounds like a careful person, not vocoder hash. Call-level acoustics still look synthetic: low `rms_cv` 0.37 (human 0.64 / synth 0.25) and very stable centroid.

## Unused features

Nothing unused would save this call without picking a known trap.

- `caller_rms_mean` says human because they are quiet. We already dropped loudness as an injection-level trap.
- Other latency stats (`pos_mean`, `pos_min`) agree with `pos_median` or call it *more* synthetic (fastest wait is 1.50 s).
- Barge-in, backchannel, and overlap would push it further toward synthetic.

## Recommendation

Leave it. This is a polite, quiet human who waits like the bots. Train has the same failure mode (`4ac923`, `c6daaa`, `d80d9f`, `e0c1f6`).

Play: `reports/tune/listen/6971-caller.wav` and `6971-agent.wav`.
